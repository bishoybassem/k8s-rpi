{
  "kind": "Pod",
  "apiVersion": "v1",
  "metadata": {
    "name": "kube-scheduler"
  },
  "spec": {
    "hostNetwork": true,
    "containers": [
      {
        "name": "kube-scheduler",
        "image": "{{ hyperkube_image_name }}:{{ hyperkube_image_tag }}",
        "command": [
          "/hyperkube",
          "scheduler",
          "--master={{ apiserver_address }}"
        ],
        "livenessProbe": {
          "httpGet": {
            "scheme": "HTTP",
            "host": "127.0.0.1",
            "port": 10251,
            "path": "/healthz"
          },
          "initialDelaySeconds": 30,
          "timeoutSeconds": 15
        }
      }
    ]
  }
}